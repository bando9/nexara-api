{
  "name": "nexara-api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "dependencies": {
    "hono": "^4.12.25"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
